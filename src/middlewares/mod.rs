//! Module declarations for `auth` middlewares.
//! This module exposes middleware functionalities for authentication checks.

/// Module for authentication middleware, including JWT validation.
pub mod auth_middlewares;